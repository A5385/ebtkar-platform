# database-warehouse

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build database-warehouse` to build the library.

## Running unit tests

Run `nx test database-warehouse` to execute the unit tests via [Vitest](https://vitest.dev/).
