# database-auth

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build database-admin` to build the library.

## Running unit tests

Run `nx test database-admin` to execute the unit tests via [Vitest](https://vitest.dev/).
